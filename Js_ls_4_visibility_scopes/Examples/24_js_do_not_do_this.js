/* Глобальная область */
function myFun() {
        /* Локальная - "область функции" */

        a = true        // т.к. внутри функции переменная не объявлена, "кладем" в глобальную
        console.log("Вывод в консоль из функции ('вроде бы' локальная 'a'): " + a)  // значение локальной переменной выводим в консоль

        /* Конец локальной области */
}

/* Глобальная область */

myFun() // Вызываем функцию, которая ничего не возвращает, но выводит в консоль
console.log("--------------------------------")

/* 
Хотя, как ранее, переменная 'a' глобально не задана, однако она и локально не объявлена,
посему при ее поиске выйдя за пределы функции в глобальную область и не найдя ее там,
например, интерпретатор браузера создаст ее как глобальную.
*/
console.log("Переменная 'a' уже глобальная: " + a)
console.log("--------------------------------")
